import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const DomainIPEnrichment = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default DomainIPEnrichment;